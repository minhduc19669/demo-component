import { Injectable } from '@angular/core';
import {IUser} from '../user/iuser';

@Injectable({
  providedIn: 'root'
})
export class UserService {
   getAll(){
   }
   findById(id){
   }
  constructor() { }
}
